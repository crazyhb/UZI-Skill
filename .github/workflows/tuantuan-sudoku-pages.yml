name: 团团数独 · 发布到 GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "apps/tuantuan-sudoku/**"
      - ".github/workflows/tuantuan-sudoku-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 核心逻辑测试
        run: node apps/tuantuan-sudoku/tests/sudoku.test.cjs

      - name: 准备站点文件
        run: |
          mkdir -p _site
          cp apps/tuantuan-sudoku/{index.html,sudoku.js,sw.js,manifest.webmanifest,icon.svg,icon-180.png,icon-512.png} _site/

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - id: deployment
        uses: actions/deploy-pages@v4
